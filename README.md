# bnglr_hpp
# dropped the colums ---> 'area_type','society','availability','balcony'
# dropped the null values
# extracted only the integers from size feature anf created new column called BHK
loaded the dataset
